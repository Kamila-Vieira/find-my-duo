import { StyleSheet } from "react-native";
import { THEME } from "../../theme";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
  },

  logo: {
    width: 214,
    height: 120,
    marginTop: 40,
    marginBottom: 28,
  },
  contentList: {
    paddingLeft: 32,
    paddingRight: 32,
  },
});
